# V1-B second physical GPU subject for the reusable Vulkan participant

Status: physical campaign complete on `inferswarm02` through the accepted V1-A seam with zero correctness-bearing generic-seam changes attempted; terminal candidate `V1B_SECOND_SUBJECT_REFACTOR_REQUIRED`, recorded in draft PR (Issue #158) pending maintainer review.

This additive investigation instantiates the accepted V1-A reusable internal execution participant for the second physical subject — the NVIDIA GA104 GeForce RTX 3060 Ti LHR (`NV-A`, BDF `04:00.0`, selector `Vulkan3`, NVIDIA ICD under Vulkan loader 1.4.309) — without touching the accepted participant, adapter, runner, or reducers. The prospective physical authority (`PHYSICAL-AUTHORITY.json`) was frozen and pushed before any V1-B physical output.

What the fresh campaign proved, entirely through the accepted unchanged seam:

- fresh qualification `v1b-nv-a-qualification-01`: PASS, 37/37 offload, no fallback, clean exit, adapter-sealed observation, capability minted only from the sealed observation (measured wall 14.913 s became the objective value; nothing was asserted);
- subject-local stability sample `v1b-nv-a-stability-01..03`: three fresh deterministic executions, all 37/37 full offload on the intended `Vulkan3`/`04:00.0` device, no fallback, clean exits, and byte-exact visible output against the prospectively frozen subject-local reference (the accepted V0-A `cor-nvavk-01` NV-A/Vulkan visible bytes, digest `9013db8f...`); the V0-B "insufficiently observed" repeatability uncertainty is resolved for this subject;
- generic planning `v1b-nv-a-plan-01`: the production generic planner selected the NV-A participant from opaque capability/contract/evidence facts; the AMD first-subject pressure resource (same opaque contract, honest accepted V1-A economics 28.59 s, real accepted qualification digest) was excluded `EVIDENCE_STALE` — its V1-A evidence was not re-qualified under this authority, and no economics were falsified to force the selection; candidate `candidate-c496226d4514325ebaded588a021ab1bfc351ede123be7725e0484dad344feb2`, frozen plan digest `c4efa685f8058b2d739c300e83f41323e0b7836a62730332cbd1c62823e5cf29`;
- canonical execution `v1b-nv-a-canonical-01` under the frozen plan: runtime exit 0, 37/37 offload, intended device proven, no fallback, and byte-exact visible output against the frozen reference (byte-exact comparator unchanged).

The single blocker, retained verbatim and not repaired: the accepted V0-C materialization/residency accounting reducer (`scripts/v0c_canonical_run.py::parse_accounting`, hash-pinned unchanged by V1-A) hardcodes the first subject's `Vulkan1` selector label in its named-buffer and memory-breakdown parsing and fails closed on the second subject's `Vulkan3` transcript (`missing or ambiguous direct accounting line: Vulkan1 model buffer size`). Per the issue's portability invariant this is a correctness-bearing change to a hash-pinned accepted source requiring separate prospective maintainer authorization; it was not made here. Every other component — generic participant, backend adapter, runner, byte-exact comparator — transferred with authority data only (see `CROSS-SUBJECT-AUDIT.json`: `AUTHORITY_DATA_ONLY` / `BACKEND_ADAPTER_DATA_ONLY`, no foundational invariant falsified).

Terminal: `V1B_SECOND_SUBJECT_REFACTOR_REQUIRED` — the second subject is physically viable (qualification, repeatability, generic selection, and byte-exact canonical output all pass), but the accepted accounting reducer requires an authorized generic repair before a valid portability PASS can be claimed. The suggested repair is fully generic: parameterize the reducer's device selector label from the frozen authority (it is already an input everywhere else), which would keep backend identity at the authority/adapter boundary.

No public API, production Vulkan-support claim, or preferred/default-backend policy is introduced; Vulkan/CUDA equivalence is not claimed and the accepted NV Vulkan-vs-CUDA output difference is not reinterpreted.

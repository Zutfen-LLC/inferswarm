# R8-A — Qwen3.8-Flash-Next state, representation, runtime, and fleet fit

Status: static/CPU-only investigation for [issue #189](https://github.com/Zutfen-LLC/inferswarm/issues/189). It records a runtime prerequisite; it is not an architecture decision, physical result, acceptance, or R8-B execution authorization.

## Authorities and scope

The separate official checkpoint and third-party GGUF authorities, their retrieval hashes, and the complete representation inventory are retained in [source-findings.md](source-findings.md). The official Qwen revision is `de4b8e4d43b917e7706784d8bb445c9af86a3540`; the Unsloth conversion revision is `38bb39ee97821de2c9009abb7e93950eec396e66`. Neither mutable repository name is used as an authority.

The [terminal reduction](terminal-reduction.json) is generated by `scripts/issue189_r8a_reducer.py` and records `R8A_QWEN38_RUNTIME_PREREQUISITE`. It is deliberately a prerequisite rather than a physical subject recommendation: the needed execution substrate has no prospectively qualified, pinned cross-resource path.

## Correction round (2026-09-14, maintainer review 5672736372)

Three additive authorities now feed the reducer; nothing is an authored aggregate constant:

- [`gguf-header-census.json`](gguf-header-census.json) — bounded metadata-only header/tensor census of the exact pinned UD-IQ1_S split set (HTTP 206 range reads, 11,077,696 bytes total, zero body bytes; raw header extents retained under `raw-headers/` and digest-bound). Establishes GGUF v3 / `qwen4exp`, `split.count=3` with 595+629 = 1224 tensors, and the exact PLE n-gram state: `per_layer_token_embd.weight` [160, 320001536] IQ4_NL = **26.82 GiB** in split 00002, plus six `blk.1.ple_*` tensors and the full `qwen4exp.ple.*` metadata family. The table is representation-separable (identity + byte extent + llama.cpp `-ot/--override-tensor` control surface at the pinned audit revision); actual placement behavior and its economics remain runtime questions.
- [`hardware-census.json`](hardware-census.json) — mechanically derived heterogeneous fleet census from read-only per-device measurement (sysfs/lspci/nvidia-smi; raw receipts under `raw-hardware/`). **Deployed accelerator memory is 96 GiB: 80 GiB NVIDIA (inferswarm01-04) + 16 GiB AMD (two Ellesmere 8 GiB on inferswarm02).** The maintainer-relayed 40 GB AMD figure was NOT confirmed — no third RX 580 and no RX 6800 XT is observable on any reachable host; both are retained as reported-not-observed with zero capacity. The pending Radeon Pro V340L (102-D05318-02) is reserve inventory modeled as two independent 8 GiB address spaces, contributing zero bytes. Valinor's GTX 1060 3GB is classified and disclosed but excluded from the execution-fleet boundary.
- The living [PCIe slot ledger](../../hardware/pcie-slot-ledger.md) was refreshed through its ordinary current-state mechanism (new measurement paragraph + topology-change-log row); no historical record was altered.

The fleet-fit conclusion is now derived per-resource: 96 GiB deployed aggregate (28.44 GiB over the 67.56 GiB representation), but no single resource holds even the 40.74 GiB PLE-free backbone (largest is the 24 GiB RTX 3090), and the 26.82 GiB PLE table alone exceeds every resource — distribution and PLE host/split residency are structurally required regardless of aggregate headroom. Aggregate VRAM remains explicitly not a feasibility proof; runtime-supported / correctness-qualified / economically-useful remain `NOT_ESTABLISHED`, and R8-mode qualified capacity is 0 bytes.

## Semantic state census

The classes below come from the pinned official config/card. **MEASURED** means a source declaration or an exact stored metadata field; it is not a runtime observation. No tensor byte total is asserted unless a header/tensor census identifies it.

| state class | semantics / authority | residency and authority consequence |
| --- | --- | --- |
| Token embedding and output head | **MEASURED:** vocabulary 248,320; embeddings are untied in the config. | Immutable weights; input/output execution locality must be explicit in a strategy. |
| N-gram / PLE table | **MEASURED:** 20M base entries, n-gram size 3, PLE at layer 2; Qwen reports 51B N-gram parameters and says it is offload-friendly. | Immutable, lookup-driven state. It may be a Memory Resource materialization/backing candidate, but this study does not prove a particular GGUF tensor or cache policy. |
| Gated DeltaNet | **MEASURED:** 36 linear-attention layers, with 48 value and 16 key heads of dimension 128. | Weights are immutable; recurrent/conv state is mutable session state and must remain with its owning execution context or have an explicit snapshot/transfer semantic. |
| Qwen Sparse Attention / indexer | **MEASURED:** 12 full-attention layers, 24 Q / 2 KV heads, and indexer budget 2,048 tokens. | Weights are immutable; KV/indexer/cache state is mutable/session scoped. |
| MoE experts, router, shared expert | **MEASURED:** 512 routed experts, 10 routed plus one shared selected per token. | Model strategy must establish expert execution/materialization legality and routing/co-residency; the generic planner must not learn these nouns. |
| Gated residual | **MEASURED:** four branches and rank 320. | Immutable parameters plus transient activations; no separate distributed authority is inferred. |
| MTP | **MEASURED:** one MTP layer; the conversion has separate MTP sidecars. | Optional speculative state is not part of a first correctness subject until its exact sidecar and protocol are pinned. |
| Vision encoder/projector | **MEASURED:** the official model is conditional-generation with a 27-depth vision configuration; conversion includes mmproj sidecars. | Excluded from any first text-only subject; multimodal correctness needs its own authority. |
| KV cache | **MEASURED:** `use_cache=true`. | Mutable session state; normally reconstructible by exact replay/prefill, but live transfer/checkpoint semantics require runtime-specific proof. Account separately from weights. |
| GDN recurrent / PLE-conv / QSA-indexer state | **INFERRED from the hybrid architecture and rollback report:** live recurrent/indexer state has distinct transfer/snapshot semantics. | Keep with the owning execution context unless an exact runtime proves snapshot, restore, or transfer equivalence; do not call it generically non-reconstructible. |
| Sampling / RNG state | **INFERRED:** mutable runtime session state, potentially replayable or checkpointable under a frozen runtime. | Not model weight state and no basis for a universal residency restriction. |
| Activation and workspace state | **INFERRED:** ephemeral allocation state. | Capacity-account separately; it is neither a persistent materialization nor a required host mirror. |

## Decomposition and pressure

| shape | R8-A classification | basis |
| --- | --- | --- |
| Contiguous GDN/QSA stages | `LEGAL_GENERIC_EXTENSION_REQUIRED` | A model strategy can expose legal stage boundaries; hand-off transports the hidden state (**CALCULATED:** 2,560 BF16 elements = 5,120 bytes/token) and must account for local mutable state. No runtime proof is claimed. |
| Expert-local execution | `MODEL_ADAPTER_EXTENSION_REQUIRED` | The model semantics make routed and shared-expert locality model-specific; no retained llama.cpp seam proves independent expert materialization/execution. |
| Hybrid stages plus expert/n-gram resources | `RUNTIME_BACKEND_PREREQUISITE` | Strategy-level legality is plausible, but the selected runtime cannot yet be shown to expose an honest, qualified control surface. |
| N-gram-only Memory Resource placement | `RUNTIME_BACKEND_PREREQUISITE` | This aligns with the Fabric Doctrine's Memory Resource distinction, but no pinned GGUF tensor census/control proves table-only host placement or its cache/prefetch semantics. |

The n-gram table is therefore not a planner special case. A later experiment must measure and bind lookup latency, access granularity, cache hit ratio, backing service rate, prefetch policy, and cache freshness before ranking any RAM/SSD/remote placement. Eventual output does not make a backing tier acceptable.

## Runtime and current-fleet fit

The smallest complete current conversion is Unsloth `UD-IQ1_S`: three split GGUF files totaling **72,546,461,344 bytes / 67.56 GiB (CALCULATED)**. This is artifact capacity, not a fitted runtime allocation. The complete verified NVIDIA pool named in the current ledger has 72 GiB of weight capacity (four RTX 3060-class 12-GiB resources plus the 24-GiB RTX 3090), leaving only 4.44 GiB before KV/recurrent state, compute buffers, staging, and per-resource fragmentation. Aggregate capacity is consequently not a fit proof.

The other deployed devices do not repair this conclusion: the RX 580 path is Vulkan/unqualified and has x1 risers, the RTX 3060 Ti capacity is not used as a fit assumption here, and a mixed-backend plan requires independent backend qualification. The PCIe ledger is a topology inventory, not a bandwidth result.

The required multi-resource/cross-host shape has no qualified llama.cpp identity. The retained audit source is `ggml-org/llama.cpp@1bc7a5af0d14b1fb72f266abbd1237b394187115`, not a selected R8-B build. The prior [#27993](https://github.com/ggml-org/llama.cpp/issues/27993) **CORRECTNESS_BLOCKER** was reported on `17252c769a63c1cb650ce98ae309cf4de0da7778`, Metal layer-split cross-host RPC, long prefill/decode, and UD-IQ4_XS; reporter rebuild `cc231cb0da565440cf6a3e5b55dfeba477972cb6` and repair `a273d22e142b9ad253a09d7b76d4d24ba64eb9bc` do not qualify NVIDIA/UD-IQ1_S or this fleet. The N-gram small-read [#28256](https://github.com/ggml-org/llama.cpp/issues/28256) is a **PERFORMANCE_ONLY_RISK** for NFS/FS-cache backing. The open multi-sequence rollback [#28019](https://github.com/ggml-org/llama.cpp/issues/28019) is a **MODE_SPECIFIC_RISK**, excluded from a first single-slot subject.

| status | R8-A conclusion |
| --- | --- |
| capacity-feasible | `NOT_ESTABLISHED` |
| runtime-supported | `NOT_ESTABLISHED` |
| correctness-qualified | `NOT_ESTABLISHED` |
| economically useful | `NOT_ESTABLISHED` |

## Required prerequisite before R8-B

Select a post-observation llama.cpp commit and exact build recipe, then separately authorize a text-only, single-slot qualification over the required NVIDIA/cross-host layer-RPC shape. Before any output is judged, freeze one complete GGUF split set (the object census exists; header/tensor identity remains required), a reference strategy, per-resource allocation limits, explicit N-gram placement/control, prompt/context ladder, and stop conditions. Demonstrate correct short and long prefill/decode on that exact shape before n-gram locality or economics work.

## Non-claims

No model/GGUF was downloaded, no header was treated as a complete representation census, no GPU/runtime was launched, and no serving, throughput, long-context, numerical-equivalence, backend, or mixed-vendor claim is made. `R8A_QWEN38_RUNTIME_PREREQUISITE` is not R8-B authorization.

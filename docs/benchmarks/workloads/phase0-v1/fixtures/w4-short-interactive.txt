InferSwarm is testing whether resident MoE experts on a second RTX 3060 can improve interactive inference over FreeToken's best single-3060 plus host-RAM path. Explain, for a technically literate contributor, why “more expert weights fit in aggregate VRAM” is not itself evidence of a speedup. Distinguish resident coverage, routing hit rate, transfer/execution cost, and end-to-end tokens per second. Keep the answer concrete and call out the single measurement that ultimately decides whether the mechanism is useful.